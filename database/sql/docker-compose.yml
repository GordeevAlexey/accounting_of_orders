version: "3.9"

services:
  db:
    container_name: db
    restart: always
    image: postgres:latest
    volumes:
      - ./pg_schema.sql:/docker-entrypoint-initdb.d/pg_schema.sql
    environment:
      - POSTGRES_PASSWORD=admin
    ports:
      - 5432:5432